## Notebook 1 proba basico

Recordar que la probabilidad se mide en un intervalo entre $[0,1]$

Las reglas basicas de la probabilidad son
* Regla de la suma
* Regla del producto

### Independencia 

El concepto de independencia es totalmente simetrico

Se dice que son independiente por que una variable no depende de la otra.

$$
P(X | Y) = P(X)
$$

## Variables Aleatorias Discretas

Funcion de masa de probabilidad 


## Valor esperado

- Discreta
    $$
    \mathbb{E}[f(x)] = \sum_{x}{f(x)\cdot p(x)}
    $$

- Continua
    $$
    \mathbb{E}[f(x)] = \int p(x)f(x) \mathrm{d}x
    $$

En una distribucion `Normal` de la siguiente manera $X \sim Normal(\mu, \sigma^2)$